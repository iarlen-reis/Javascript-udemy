/*
Aprendendo sobre o objeto Math:
    - Com o metodo floor() podemos aredondar um número flutuante
        para baixo.
    - Usando o metodo .ceil() podemos arendondar um número flutuante
        para cima.
    - Usando o metodo round() podemos arendondar um número flutuante para
        o número inteiro mais proximo dele.
    - Podemos pegar o maior número de uma sequência de número, usando o
        metodo Max(numbers)
    - podemos usar o min(numbers) para pegar o menor número de uma sequência.
    - Podemos usar o metodo random() para gerar números aleatoriamente que
        vai de 0 até 1.
*/

let num1 = 9;

console.log(10 / 0);
